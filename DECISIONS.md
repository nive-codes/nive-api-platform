# Architectural Decisions

이 문서는 본 플랫폼을 설계하며 내린 주요 기술적 결정과
그 배경을 기록합니다.
각 결정은 실제 프로젝트 수행과 운영 과정에서 겪었던 문제들을 기반으로 합니다.
궁금한 사항은 https://nive-codes.tistory.com에서 확인 바랍니다.

## Why not traditional Service Layer?
전통적인 Service 계층 구조는 초기에는 단순하고 이해하기 쉽지만,
프로젝트가 커질수록 다음과 같은 문제를 반복적으로 경험했습니다.

- Service 클래스의 비대화
- Service 내부에 축적되는 중복 헬퍼 메서드
- 역할과 의미를 잃은 외부 헬퍼 클래스의 증가
- 비즈니스 책임의 경계가 점점 모호해짐
- 단위 테스트 작성의 어려움
- 특정 기능 변경 시 예상 범위를 넘는 연쇄적인 영향 발생

이를 해결하기 위해 본 플랫폼에서는
비즈니스 로직을 Service 단위가 아닌,
UseCase 단위의 “행동”으로 분리했습니다.

UseCase는 “무엇을 할 수 있는가”를 기준으로 정의되며,
플랫폼 공통 성격의 행위와
도메인별 행위를 명확히 구분할 수 있도록 설계되었습니다.

이 구조는 기능 확장 시에도
기존 로직에 대한 영향 범위를 최소화하는 것을 목표로 합니다.

## Why JWT + Stateless?
본 플랫폼은 특정 서비스 구현에 국한되지 않고,
확장 가능한 구조를 전제로 설계되었습니다.

이를 위해 인증 방식은
세션 기반이 아닌 JWT 기반의 Stateless 구조를 채택했습니다.

- 서버 간 상태 공유가 필요 없음
- 수평 확장에 유리
- 외부 시스템 및 MSA 환경과의 연동 용이

인증 로직 또한 플랫폼 레벨의 책임으로 두어,
개별 서비스가 인증 구현에 직접 의존하지 않도록 설계했습니다.

## Why multi-module?
본 플랫폼에서는 배포 단위의 분리보다
장기적인 유지보수 비용과 개발 생산성을 더 중요하게 판단했습니다.

이를 위해 최소한의 모듈 분리를 통해
각 계층의 책임을 명확히 하되,
불필요한 모듈 간 이동 비용은 의도적으로 줄였습니다.

- common: 플랫폼 공통 규칙과 계약
- domain: 비즈니스 핵심 규칙
- application: UseCase 중심의 비즈니스 흐름과 API 진입점
- web: 기술 의존 영역 (Filter, Security, Config, Interceptor 등)
  application 모듈은 플랫폼의 중심으로,
  UseCase와 함께 adapter.web 형태의 Controller를 포함합니다.

이는 초기 개발 및 기능 확장 시
모듈 간 이동 비용을 최소화하고,
비즈니스 흐름을 빠르게 파악할 수 있도록 하기 위한 선택입니다.

web 모듈은 HTTP, Security, Filter 등
기술적인 관심사를 전담하며,
비즈니스 코드와의 결합을 최소화합니다.

이 구조는 단일 책임 원칙을 유지하면서도,
플랫폼 기반 개발에서 요구되는
속도와 확장성 사이의 균형을 목표로 합니다.
