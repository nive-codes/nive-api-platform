# Module Responsibilities


> 이 섹션은 본 플랫폼의 멀티 모듈 구성을 요약합니다.
> 각 모듈은 명확한 책임을 가지며,
> 플랫폼이 서버로 기동되고 확장되기까지의 역할을 분리합니다.

아래 내용은 개요이며,
각 모듈의 상세 설계는 별도의 아키텍처 문서에서 다룹니다.

## common

common 모듈은
플랫폼 전반에서 공유되는 규칙과 계약(contract)을 정의하는
기준 레이어입니다.

이 모듈은 비즈니스 로직이나 기술 구현을 포함하지 않으며,
각 계층이 동일한 기준으로 동작하도록
플랫폼의 공통 언어와 정책을 제공합니다.

주요 책임:
- 표준 API 응답 규칙 정의
- 공통 에러 코드 및 예외 계층 정의
- 검증 정책(CommonValidator) 및 보조 유틸리티 제공
- 플랫폼 전반에서 사용되는 공통 타입과 계약 제공

> common 모듈은  
> 모든 계층에서 참조될 수 있지만,  
> 어떤 계층에도 의존하지 않습니다.


## domain

domain 모듈은
플랫폼이 서버로서 동작하기 위해 반드시 필요한
핵심 비즈니스 엔티티와 영속 모델을 정의합니다.

이 모듈은 단순한 비즈니스 도메인 집합이 아니라,
인증, 보안, 운영, 로그 등
서버 기동 시점부터 필요한
플랫폼 필수 도메인들을 포함합니다.

주요 책임:
- 사용자, 권한, 인증 상태에 대한 핵심 모델 정의
- 토큰, MFA, IP 차단 등 보안 관련 도메인 영속화
- 접근/로그/감사 도메인 관리
- 시스템 초기 구동에 필요한 기준 엔티티 제공

domain 모듈은
common 모듈에만 의존하며,
application / web 모듈에는 의존하지 않습니다.


## application

application 모듈은
UseCase 단위로 비즈니스 행위를 정의하고,
플랫폼의 비즈니스 흐름을 실제로 조직하는
코어 레이어입니다.

이 모듈은:
- UseCase 중심의 비즈니스 로직 실행
- 외부 연동(integration) 관리
- Port / Policy를 통한 의존성 제어
- 플랫폼 기본 도메인 UseCase 제공

> 특히, application.adapter.web 하위에 Controller를 포함하여
> API 진입점과 UseCase를 가까이 배치함으로써,
> 초기 개발 생산성과 흐름 가독성을 확보합니다.

> 이 설계는:
> - 빠른 기능 개발
> - 모듈 간 이동 비용 최소화
> - 비즈니스 흐름의 명확한 파악
>   을 고려한 선택입니다.

```text
UseCase 형태로 구성된 이유는
플랫폼 공통으로 재사용될 여지가 높은 비즈니스 행위를
명확히 분리하기 위함입니다.

필요한 경우,
기존의 3-Tier(Service 중심) 구조 또한
application 모듈 내에서 충분히 구현 가능합니다.
```


## web

web 모듈은
플랫폼의 가장 바깥에서
기술적 관심사를 실제로 실행하는 레이어입니다.

이 모듈은 비즈니스 로직을 포함하지 않으며,
요청의 진입부터 비즈니스 로직 진입 직전까지의
모든 기술 처리를 담당합니다.


주요 책임:
- 인증 / 인가 처리 (JWT, Security)
- Filter / Interceptor 기반 요청 제어
- 트래픽 제어 및 접근 제한
- 로깅, 감사(Audit), 예외 매핑
- Spring Web / Security 설정

```text
web 모듈은
기술 정책을 선언하는 곳이 아니라,
이를 실제로 실행하고 강제하는 레이어입니다.
```

web 모듈은
기술 변경에 유리하도록
비즈니스 코드와 명확히 분리되어 있습니다.


## flow
```text
1. Client Request
2. web.Filter / Interceptor
   - 인증 / 인가
   - 트래픽 제어
   - 로깅 / 감사
3. application.adapter.web.Controller
4. UseCase 실행
5. Domain 엔티티 및 규칙 적용
6. common Response / Error Contract 반환
```


> 이 문서는 플랫폼 아키텍처의 요약이며,
> 각 모듈의 상세 내용은 ARCHITECTURE_*.md 문서를 참고합니다.
